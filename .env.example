# Copy to .env for local experiments. .env is git-ignored and must never be
# committed. Nothing here is required to run version 1 of the agent.

# Only needed if you set ai.provider to "openai" in config.yaml.
# OPENAI_API_KEY=

# Point the agent at an alternative config file.
# PRI_CONFIG=config.local.yaml
